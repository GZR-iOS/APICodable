Install test server steps:
- Install HTTP server (with PHP) - eg. XAMPP
- Configure so "simple.php" is connectable.
- Create a folder named "Files" next to "simple.php" with permission 777 to store uploaded files.
